const isElement = (ast) => {
    return ast.type === 'element';
};
const getTextContent = (ast, defaultStr = '') => {
    if (!ast) {
        return defaultStr;
    }
    switch (ast.type) {
        case 'text': {
            return ast.value.replace(/\s+/g, ' ');
        }
        case 'element': {
            switch (ast.tagName) {
                case 'br': {
                    return '\n';
                }
            }
            return ast.children.map(child => getTextContent(child)).join('');
        }
    }
    return defaultStr;
};
const getElementChildren = (ast) => {
    if (!ast) {
        return [];
    }
    if (ast.type === 'element') {
        return ast.children.filter(child => child.type === 'element');
    }
    return [];
};
const getTextChildren = (ast) => {
    if (!ast) {
        return [];
    }
    if (ast.type === 'element') {
        return ast.children.filter(child => child.type === 'text');
    }
    return [];
};
const getTextChildrenOnlyAst = (ast) => {
    return {
        ...ast,
        children: getTextChildren(ast),
    };
};
const isTagInline = (tagName) => {
    // Phrasing content
    const inlineElements = [
        'a',
        'abbr',
        'audio',
        'b',
        'bdi',
        'bdo',
        'br',
        'button',
        'canvas',
        'cite',
        'code',
        'data',
        'datalist',
        'del',
        'dfn',
        'em',
        'embed',
        'i',
        // 'iframe' is not included because it needs special handling
        // 'img' is not included because it needs special handling
        'input',
        'ins',
        'kbd',
        'label',
        'link',
        'map',
        'mark',
        'math',
        'meta',
        'meter',
        'noscript',
        'object',
        'output',
        'picture',
        'progress',
        'q',
        'ruby',
        's',
        'samp',
        'script',
        'select',
        'slot',
        'small',
        'span',
        'strong',
        'sub',
        'sup',
        'svg',
        'template',
        'textarea',
        'time',
        'u',
        'var',
        'video',
        'wbr',
    ];
    return inlineElements.includes(tagName);
};
const isElementInline = (element) => {
    return (isTagInline(element.tagName) ||
        // Inline elements
        !!(typeof element.properties?.style === 'string' &&
            element.properties.style.match(/display:\s*inline/)));
};
const getInlineElementsAndText = (ast) => {
    if (!ast || !ast.children) {
        return [];
    }
    return ast.children.filter((child) => {
        if (child.type === 'text') {
            return true;
        }
        if (child.type === 'element' && child.tagName && isElementInline(child)) {
            return true;
        }
        return false;
    });
};
const getInlineOnlyElementAST = (ast) => {
    return {
        ...ast,
        children: getInlineElementsAndText(ast),
    };
};
const querySelectorTag = (ast, tagName) => {
    if (ast.type === 'element') {
        if (ast.tagName === tagName) {
            return ast;
        }
        for (const child of ast.children) {
            const result = querySelectorTag(child, tagName);
            if (result) {
                return result;
            }
        }
    }
    return undefined;
};
const querySelectorClass = (ast, className) => {
    if (ast.type === 'element') {
        if (Array.isArray(ast.properties?.className) &&
            ast.properties.className.includes(className)) {
            return ast;
        }
        for (const child of ast.children) {
            const result = querySelectorClass(child, className);
            if (result) {
                return result;
            }
        }
    }
    return undefined;
};
const querySelectorId = (ast, id) => {
    if (ast.type === 'element') {
        if (ast.properties.id === id) {
            return ast;
        }
        for (const child of ast.children) {
            const result = querySelectorId(child, id);
            if (result) {
                return result;
            }
        }
    }
    return undefined;
};
const querySelector = (ast, selector) => {
    if (ast.type === 'root') {
        for (const child of ast.children) {
            const result = querySelector(child, selector);
            if (result) {
                return result;
            }
        }
    }
    else if (ast.type === 'element') {
        if (selector.startsWith('.')) {
            return querySelectorClass(ast, selector.slice(1));
        }
        else if (selector.startsWith('#')) {
            return querySelectorId(ast, selector.slice(1));
        }
        else {
            return querySelectorTag(ast, selector);
        }
    }
    return undefined;
};
const flatNodes = (ast, expression) => {
    if (ast.type === 'element') {
        const children = ast.children.map(child => flatNodes(child, expression));
        return {
            ...ast,
            children: children.flatMap(child => {
                if (child.type === 'element') {
                    if (expression(child.tagName)) {
                        return child.children;
                    }
                }
                return child;
            }),
        };
    }
    return ast;
};
// Check if it is a paragraph like element
// https://html.spec.whatwg.org/#paragraph
const isParagraphLike = (node) => {
    // Flex container
    return ((typeof node.properties?.style === 'string' &&
        node.properties.style.match(/display:\s*flex/) !== null) ||
        getElementChildren(node).every(child => isElementInline(child)));
};
export const HastUtils = {
    isElement,
    getTextContent,
    getElementChildren,
    getTextChildren,
    getTextChildrenOnlyAst,
    getInlineOnlyElementAST,
    querySelector,
    flatNodes,
    isParagraphLike,
};
//# sourceMappingURL=hast.js.map