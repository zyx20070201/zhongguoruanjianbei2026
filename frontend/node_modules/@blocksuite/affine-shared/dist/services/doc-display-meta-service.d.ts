import type { AliasInfo, ReferenceParams } from '@blocksuite/affine-model';
import type { Disposable } from '@blocksuite/global/utils';
import type { Doc } from '@blocksuite/store';
import type { TemplateResult } from 'lit';
import { LifeCycleWatcher } from '@blocksuite/block-std';
import { type Container } from '@blocksuite/global/di';
import { type Signal } from '@preact/signals-core';
export type DocDisplayMetaParams = {
    referenced?: boolean;
    params?: ReferenceParams;
} & AliasInfo;
/**
 * Customize document display title and icon.
 *
 * Supports the following blocks:
 *
 * * Inline View:
 *      `AffineReference`
 * * Card View:
 *      `EmbedLinkedDocBlockComponent`
 *      `EmbedEdgelessLinkedDocBlockComponent`
 * * Embed View:
 *      `EmbedSyncedDocBlockComponent`
 *      `EmbedEdgelessSyncedDocBlockComponent`
 */
export interface DocDisplayMetaExtension {
    icon: (docId: string, referenceInfo?: DocDisplayMetaParams) => Signal<TemplateResult>;
    title: (docId: string, referenceInfo?: DocDisplayMetaParams) => Signal<string>;
}
export declare const DocDisplayMetaProvider: import("@blocksuite/global/di").ServiceIdentifier<DocDisplayMetaExtension> & ((variant: import("@blocksuite/global/di").ServiceVariant) => import("@blocksuite/global/di").ServiceIdentifier<DocDisplayMetaExtension>);
export declare class DocDisplayMetaService extends LifeCycleWatcher implements DocDisplayMetaExtension {
    static icons: {
        readonly deleted: TemplateResult<1>;
        readonly aliased: TemplateResult<1>;
        readonly page: TemplateResult<1>;
        readonly edgeless: TemplateResult<1>;
        readonly linkedBlock: TemplateResult<1>;
        readonly linkedPage: TemplateResult<1>;
        readonly linkedEdgeless: TemplateResult<1>;
    };
    static key: string;
    readonly disposables: Disposable[];
    readonly iconMap: WeakMap<Doc, Signal<TemplateResult>>;
    readonly titleMap: WeakMap<Doc, Signal<string>>;
    static setup(di: Container): void;
    dispose(): void;
    icon(pageId: string, { params, title, referenced }?: DocDisplayMetaParams): Signal<TemplateResult>;
    title(pageId: string, { title }?: DocDisplayMetaParams): Signal<string>;
    unmounted(): void;
}
//# sourceMappingURL=doc-display-meta-service.d.ts.map