import { type Color, ColorScheme } from '@blocksuite/affine-model';
import { type BlockStdScope, Extension, type ExtensionType } from '@blocksuite/block-std';
import { type Container } from '@blocksuite/global/di';
import { type Signal } from '@preact/signals-core';
export declare const ThemeExtensionIdentifier: import("@blocksuite/global/di").ServiceIdentifier<ThemeExtension> & ((variant: import("@blocksuite/global/di").ServiceVariant) => import("@blocksuite/global/di").ServiceIdentifier<ThemeExtension>);
export interface ThemeExtension {
    getAppTheme?: () => Signal<ColorScheme>;
    getEdgelessTheme?: (docId?: string) => Signal<ColorScheme>;
}
export declare function OverrideThemeExtension(service: ThemeExtension): ExtensionType;
export declare const ThemeProvider: import("@blocksuite/global/di").ServiceIdentifier<ThemeService> & ((variant: import("@blocksuite/global/di").ServiceVariant) => import("@blocksuite/global/di").ServiceIdentifier<ThemeService>);
export declare class ThemeService extends Extension {
    private std;
    app$: Signal<ColorScheme>;
    edgeless$: Signal<ColorScheme>;
    get appTheme(): ColorScheme;
    get edgelessTheme(): ColorScheme;
    get theme(): ColorScheme;
    get theme$(): Signal<ColorScheme>;
    constructor(std: BlockStdScope);
    static setup(di: Container): void;
    /**
     * Generates a CSS's color property with `var` or `light-dark` functions.
     *
     * Sometimes used to set the frame/note background.
     *
     * @param color - A color value.
     * @param fallback  - If color value processing fails, it will be used as a fallback.
     * @returns - A color property string.
     *
     * @example
     *
     * ```
     * `rgba(255,0,0)`
     * `#fff`
     * `light-dark(#fff, #000)`
     * `var(--affine-palette-shape-blue)`
     * ```
     */
    generateColorProperty(color: Color, fallback?: string, theme?: ColorScheme): string;
    /**
     * Gets a color with the current theme.
     *
     * @param color - A color value.
     * @param fallback - If color value processing fails, it will be used as a fallback.
     * @param real - If true, it returns the computed style.
     * @returns - A color property string.
     *
     * @example
     *
     * ```
     * `rgba(255,0,0)`
     * `#fff`
     * `--affine-palette-shape-blue`
     * ```
     */
    getColorValue(color: Color, fallback?: string, real?: boolean, theme?: ColorScheme): string;
    getCssVariableColor(property: string, theme?: ColorScheme): string;
}
export declare class ThemeObserver {
    private observer;
    theme$: Signal<ColorScheme>;
    constructor();
    destroy(): void;
}
export declare const getThemeObserver: () => ThemeObserver;
//# sourceMappingURL=theme-service.d.ts.map