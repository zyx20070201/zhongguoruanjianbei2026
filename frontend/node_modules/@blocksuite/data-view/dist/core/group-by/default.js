import { groupByMatcher } from './matcher.js';
export const defaultGroupBy = (dataSource, propertyMeta, propertyId, data) => {
    const name = groupByMatcher.match(propertyMeta.config.type({ data, dataSource }))?.name;
    return name != null
        ? {
            type: 'groupBy',
            columnId: propertyId,
            name: name,
        }
        : undefined;
};
//# sourceMappingURL=default.js.map