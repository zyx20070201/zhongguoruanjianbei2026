import type { Coordinates, DistanceMeasurement, DndSession, DndSessionProps, Sensor } from '../types.js';
import { Listeners } from '../utils/listeners.js';
interface DistanceConstraint {
    distance: DistanceMeasurement;
}
export type PointerActivationConstraint = DistanceConstraint;
export type MouseSensorProps = {
    activationConstraint?: PointerActivationConstraint;
};
export declare const mouseSensor: Sensor<MouseSensorProps>;
export declare function hasViewportRelativeCoordinates(event: Event): event is Event & Pick<PointerEvent, 'clientX' | 'clientY'>;
export declare class MouseSession implements DndSession {
    private sessionProps;
    private props;
    private activated;
    autoScrollEnabled: boolean;
    documentListeners: Listeners<Document>;
    handleCancel: () => void;
    handleEnd: () => void;
    handleKeydown: (event: KeyboardEvent) => void;
    handleMove: (event: Event) => void;
    handleStart: () => void;
    initialCoordinates: Coordinates;
    removeTextSelection: () => void;
    windowListeners: Listeners<Window & typeof globalThis>;
    constructor(event: Event, sessionProps: DndSessionProps, props: MouseSensorProps);
    private attach;
    private detach;
}
export {};
//# sourceMappingURL=mouse.d.ts.map