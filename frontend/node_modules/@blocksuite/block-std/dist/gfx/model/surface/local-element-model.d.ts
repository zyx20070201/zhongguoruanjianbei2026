import type { IVec, SerializedXYWH, XYWH } from '@blocksuite/global/utils';
import { Bound, PointLocation } from '@blocksuite/global/utils';
import type { EditorHost } from '../../../view/index.js';
import type { GfxCompatibleInterface, PointTestOptions } from '../base.js';
import type { GfxGroupModel } from '../model.js';
import type { SurfaceBlockModel } from './surface-model.js';
export declare function prop<V, T extends GfxLocalElementModel>(): (_target: ClassAccessorDecoratorTarget<T, V>, context: ClassAccessorDecoratorContext) => ClassAccessorDecoratorResult<T, V>;
export declare abstract class GfxLocalElementModel implements GfxCompatibleInterface {
    private _mutex;
    protected _local: Map<string | symbol, unknown>;
    /**
     * Used to store all the name of the properties that have been decorated
     * with the `@prop`
     */
    protected _props: Set<string | symbol>;
    protected _surface: SurfaceBlockModel;
    /**
     * used to store the properties' cache key
     * when the properties required heavy computation
     */
    cache: Map<string | symbol, unknown>;
    id: string;
    abstract readonly type: string;
    get deserializedXYWH(): XYWH;
    get elementBound(): Bound;
    get group(): GfxGroupModel | null;
    get groups(): GfxGroupModel[];
    get h(): number;
    get responseBound(): Bound;
    get surface(): SurfaceBlockModel;
    get w(): number;
    get x(): number;
    get y(): number;
    constructor(surfaceModel: SurfaceBlockModel);
    containsBound(bounds: Bound): boolean;
    getLineIntersections(start: IVec, end: IVec): PointLocation[] | null;
    getNearestPoint(point: IVec): IVec;
    getRelativePointLocation(relativePoint: IVec): PointLocation;
    includesPoint(x: number, y: number, opt: PointTestOptions, __: EditorHost): boolean;
    intersectsBound(bound: Bound): boolean;
    isLocked(): boolean;
    isLockedByAncestor(): boolean;
    isLockedBySelf(): boolean;
    lock(): void;
    unlock(): void;
    accessor groupId: string;
    accessor hidden: boolean;
    accessor index: string;
    accessor opacity: number;
    accessor responseExtension: [number, number];
    accessor rotate: number;
    accessor seed: number;
    accessor xywh: SerializedXYWH;
}
//# sourceMappingURL=local-element-model.d.ts.map