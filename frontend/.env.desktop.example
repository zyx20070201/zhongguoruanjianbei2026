# Copy this file to .env.desktop before building the packaged desktop app.
# The desktop shell is cloud-first: UI is local, API requests still go to the server.
VITE_API_BASE_URL=https://your-server.example/api
